import React, { FC } from 'react';
import './Loader.css';

export const Loader: FC<{}> = ({}) => {
    return (
        <div className={'DiagramLoader'}>
            Loading...
        </div>
    )
};